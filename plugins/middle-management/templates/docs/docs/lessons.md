# Lessons

Created: <YYYY-MM-DD HH:MM> · <model-id> · direct
Last updated: <YYYY-MM-DD HH:MM> · <model-id>

Durable knowledge, so the next session does not repeat the mistake. Each lesson is **What / Why /
Fix**. Read the entries covering an area before working in it; the wrap routine appends new ones
at the end of a session.

**Write discipline:** facts, not history. No dated status entries ("built X, commit Y") — that is
the machine's session log. When a lesson is superseded, rewrite it; never stack a "RESOLVED" on
top of stale text. One heading per lesson, newest at the bottom.

---

## <Short name of the lesson>

- **What:** <what happened, in one or two sentences — the observable failure, not the theory>
- **Why:** <the root cause, once it was actually established>
- **Fix:** <what to do instead, concretely enough to follow without this session's context>
